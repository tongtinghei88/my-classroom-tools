# Firebase Notes

Default Firebase project: `ai-agent48`

Firestore demo collections currently opened by rules:

- `wordcloud_words`
- `test_collection`

Keep Firestore rules narrow. Do not open every collection for public writes.

